// Declaration
function sayHi(name) {
  return `Hi ${name}`;
}

// Expression -> masuk ke variable
const sayHello = function (name) {
  return `Hello ${name}`;
};

// Arrow Funct
const sapa = (name) => `HAIII ${name.toUpperCase()}`;


console.log(sapa());